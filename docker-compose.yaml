services:
  server:
    build:
      context: .
      dockerfile: docker/server.Dockerfile
    network_mode: "host"
    command: ["python", "-m", "src.server"]      # server binds 127.0.0.1:1234

  client:
    build:
      context: .
      dockerfile: docker/client.Dockerfile
    network_mode: "host"
    environment:
      HOSTFULL_NAME: "localhost"
      PORT: "1234"
    command: ["python", "-m", "src.client"]      # client connects to 127.0.0.1:1234


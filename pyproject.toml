[project]
name = "tls-connection-coding-challenge"
version = "0.1.0"
dependencies = ["cryptography", "trustme"]  # runtime deps (example)

[project.optional-dependencies]
dev = [
  "pytest",
  "pytest-timeout",
  "pytest-cov",
  "flake8",
  "mypy",
  "sphinx",
  "sphinxcontrib-mermaid",
]
